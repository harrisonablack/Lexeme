import SwiftUI

struct DefinitionView: View {
    var body: some View {
        VStack {
            Text("Definition")
                .font(.title)
                .fontWeight(.bold)
                .padding(.top, 20)
        }
    }
}
